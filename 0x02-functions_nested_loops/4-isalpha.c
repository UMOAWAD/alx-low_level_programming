#include "main.h"
#include <ctype.h>

/**
 * _isalpha - Returns 1 if the parameter is a letter.
 *
 * @c: The parameter to check.
 *
 * Return: 0 or 1
 */

int isalpha(int c)
{
	if (isalpha(c))
	{
		return (1);
	}

	return (0);
}
