#include "main.h"
#include <stdlib.h>

/**
 * _abs - Calculates the absolute value of an integer.
 *
 * @i: The integer to compute the absolute value for.
 * Retrun: abs
 */

int _abs(int i)
{
	return (abs(i));
}
